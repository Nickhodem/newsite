django-admin startproject nazwaprojektu #tworzy nowy folder z potrzebnymi plikami
manage.py startapp nazwaaplikacji #tworzy nowy folder z aplikacja
w settings.py dodajemy do Installed App nazwe apki (nazwe folderu)
w pliku models.py dodajemy class naszej apki
w pliku views.py dodajemy http response
				from django.http import HttpResponse

				def index(request):
				    return HttpResponse('hello')
    



